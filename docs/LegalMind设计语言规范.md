# 🎨 LegalMind 设计语言规范

> 基于 LegalMind-Arbitration 仲裁系统 UI/UX 分析，总结升华的设计体系

---

## 🌟 一、设计理念

### 核心哲学

**"Authority through Design, Efficiency through Intelligence"**
**"设计彰显权威，智能驱动效率"**

LegalMind 设计语言追求的不是传统政务系统的严肃冰冷，而是打造**Premium SaaS 级别**的现代化法律科技体验，对标 Linear、Raycast、Figma 等顶级产品。

### 三大设计原则

| 原则 | 描述 | 实践 |
|------|------|------|
| **专业权威** | 传达法律行业的严谨与可信 | 深色调搭配品牌橙、清晰的层级结构 |
| **高效智能** | AI 驱动的智能化体验 | 智能搜索、快捷键支持、上下文感知 |
| **现代优雅** | Premium SaaS 视觉语言 | 渐变、玻璃态、微动效、流畅过渡 |

---

## 🎨 二、视觉系统

### 色彩体系

#### 品牌主色 - LegalMind 橙

| 层级 | 色值 | 用途 |
|------|------|------|
| **Primary-50** | `#FFF4F1` | 悬停背景、高亮区域 |
| **Primary-100** | `#FFE6DC` | 浅色背景、选中态 |
| **Primary-200** | `#FFCCB8` | 次要边框 |
| **Primary-300** | `#FFB394` | 禁用态强调 |
| **Primary-400** | `#FF9970` | 次要按钮悬停 |
| **Primary-500** | `#FF6B35` | **核心品牌色**（主按钮、焦点状态） |
| **Primary-600** | `#E55A2B` | 按钮悬停态 |
| **Primary-700** | `#CC4A1F` | 按钮按下态 |
| **Primary-800** | `#B23A13` | 深色强调 |
| **Primary-900** | `#992A07` | 极深强调 |

```css
/* 渐变应用示例 */
background: linear-gradient(135deg, #FF6B35, #E55A2B);
box-shadow: 0 4px 12px rgba(255, 107, 53, 0.15);
```

#### 中性色阶

| 层级 | 色值 | 用途 |
|------|------|------|
| **Neutral-50** | `#F8F9FA` | 页面背景 |
| **Neutral-100** | `#F1F3F4` | 卡片背景、输入框 |
| **Neutral-200** | `#E9ECEF` | 边框、分割线 |
| **Neutral-300** | `#DEE2E6` | 禁用边框 |
| **Neutral-400** | `#CED4DA` | 占位符文字 |
| **Neutral-500** | `#ADB5BD` | 辅助文字 |
| **Neutral-600** | `#6C757D` | 次要文字 |
| **Neutral-700** | `#495057` | 正文文字 |
| **Neutral-800** | `#343A40` | 标题文字 |
| **Neutral-900** | `#212529` | 主标题 |

#### 功能色

| 类型 | 色值 | 应用场景 |
|------|------|----------|
| **Success** | `#28A745` → `#22C55E` | 成功状态、完成标记 |
| **Warning** | `#FFC107` → `#F59E0B` | 警告提示、待处理状态 |
| **Error** | `#DC3545` → `#EF4444` | 错误状态、危险操作 |
| **Info** | `#17A2B8` → `#3B82F6` | 信息提示、链接 |

#### 业务节点色（仲裁工作台专用）

```css
--node-document: #3498db;      /* 文书节点 - 蓝色 */
--node-ai: #27ae60;            /* AI节点 - 绿色 */
--node-hearing: #e74c3c;       /* 庭审节点 - 红色 */
--node-timeline: #9b59b6;      /* 时间线节点 - 紫色 */
--node-collaboration: #f39c12; /* 协作节点 - 橙黄色 */
```

---

### 排版系统

#### 字体栈

```css
font-family: 'Inter', 'PingFang SC', -apple-system, BlinkMacSystemFont, 
             'Segoe UI', 'Roboto', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
```

#### 字体尺寸

| Token | 尺寸 | 行高 | 用途 |
|-------|------|------|------|
| `text-xs` | 12px | 16px | 辅助信息、时间戳 |
| `text-sm` | 14px | 20px | 正文、按钮 |
| `text-base` | 16px | 24px | 大正文 |
| `text-lg` | 18px | 28px | 小标题 |
| `text-xl` | 20px | 28px | 卡片标题 |
| `text-2xl` | 24px | 32px | 页面标题 |
| `text-3xl` | 30px | 36px | 大标题 |
| `text-4xl` | 36px | 40px | 主视觉标题 |

---

### 间距与圆角

#### 间距规范

```css
spacing: {
  xs: 4px,   /* 0.25rem */
  sm: 8px,   /* 0.5rem */
  md: 16px,  /* 1rem */
  lg: 24px,  /* 1.5rem */
  xl: 32px,  /* 2rem */
  2xl: 48px  /* 3rem */
}
```

#### 圆角规范

```css
borderRadius: {
  sm: 4px,      /* 小型元素：标签、徽章 */
  md: 8px,      /* 中型元素：输入框、小卡片 */
  lg: 12px,     /* 大型元素：卡片、面板 (0.75rem) */
  xl: 16px,     /* 特大元素：模态框、弹窗 */
  full: 9999px  /* 圆形：头像、按钮图标 */
}
```

---

### 阴影系统

```css
/* 品牌阴影 */
--shadow-brand: 0 4px 12px rgba(255, 107, 53, 0.15);
--shadow-brand-lg: 0 8px 24px rgba(255, 107, 53, 0.2);

/* 通用阴影 */
--shadow-card: 0 2px 8px rgba(0, 0, 0, 0.08);
--shadow-card-hover: 0 4px 16px rgba(0, 0, 0, 0.12);
--shadow-lg: 0 8px 30px rgba(0, 0, 0, 0.15);
```

---

## ✨ 三、动效系统

### 核心动效哲学

- **自然流畅**：使用 `cubic-bezier(0.4, 0, 0.2, 1)` 缓动
- **恰到好处**：动效时长控制在 200-500ms
- **有意义的反馈**：每个交互都有视觉响应

### 基础过渡

```css
/* 全局默认过渡 */
transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);

/* 标准动画时长 */
--duration-fast: 150ms;
--duration-normal: 200ms;
--duration-slow: 300ms;
--duration-slower: 500ms;
```

### 关键帧动画

#### 淡入淡出

```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes fadeOut {
  from { opacity: 1; }
  to { opacity: 0; }
}
```

#### 滑动效果

```css
@keyframes slideUp {
  from { 
    opacity: 0; 
    transform: translateY(20px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}
```

#### 缩放效果

```css
@keyframes scaleIn {
  from { 
    opacity: 0; 
    transform: scale(0.9); 
  }
  to { 
    opacity: 1; 
    transform: scale(1); 
  }
}
```

#### 品牌脉冲

```css
@keyframes pulsePrimary {
  0%, 100% { box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.7); }
  70% { box-shadow: 0 0 0 10px rgba(255, 107, 53, 0); }
}
```

### 微交互规范

| 元素 | 交互 | 动效 |
|------|------|------|
| **按钮** | Hover | `scale(1.02) + translateY(-2px) + shadow` |
| **按钮** | Active | `scale(0.98)` |
| **卡片** | Hover | `translateY(-4px) + 顶部橙色边框滑入` |
| **输入框** | Focus | `scale(1.02) + 橙色边框 + 光晕` |
| **导航项** | Hover | `translateX(8px) + 渐变背景` |
| **头像** | Hover | `scale(1.1)` |

---

## 🧩 四、组件规范

### 按钮 (Button)

#### 变体

| 变体 | 样式 | 用途 |
|------|------|------|
| **Default** | 橙色渐变背景、白色文字 | 主要操作 |
| **Outline** | 白色背景、灰色边框 → Hover 变橙色 | 次要操作 |
| **Ghost** | 透明背景 → Hover 浅橙色 | 工具栏按钮 |
| **Destructive** | 红色背景 | 危险操作 |
| **Link** | 橙色文字下划线 | 链接样式 |

```tsx
// 主按钮样式
className="bg-gradient-to-r from-orange-500 to-orange-600 text-white 
           shadow-lg hover:shadow-xl hover:scale-105 active:scale-95 
           hover:from-orange-600 hover:to-orange-700"
```

### 卡片 (Card)

```css
/* 基础样式 */
.card {
  background-color: #FFFFFF;
  border: 1px solid #E9ECEF;
  border-radius: 0.75rem;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

/* 悬停效果 - 顶部橙色装饰条滑入 */
.card::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 3px;
  background: linear-gradient(90deg, #FF6B35, #E55A2B);
  transform: scaleX(0);
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
}

.card:hover::before {
  transform: scaleX(1);
}
```

### 输入框 (Input)

```css
/* 焦点状态 */
.input:focus {
  border-color: #FF6B35;
  box-shadow: 0 0 0 3px rgba(255, 107, 53, 0.1);
  background-color: #FFFFFF;
  transform: scale(1.02);
  outline: none;
}
```

### 导航项 (Nav Item)

```css
.nav-item:hover {
  background: linear-gradient(135deg, #FFF4F1, #FFE6DC);
  transform: translateX(8px);
  color: #CC4A1F;
}

.nav-item.active {
  background: linear-gradient(135deg, #FFE6DC, #FFCCB8);
  color: #B23A13;
  font-weight: 600;
}

/* 左侧激活指示条 */
.nav-item.active::before {
  width: 3px;
  background: #FF6B35;
  transform: scaleY(1);
}
```

---

## 📐 五、布局模式

### 工作台布局 (Workspace)

```
┌─────────────────────────────────────────────────────────────┐
│ Header (60px) - 全局搜索 / 角色切换 / 通知 / 用户菜单        │
├────────────┬────────────────────────────────────────────────┤
│            │                                                │
│  Sidebar   │              Main Content                      │
│  (280px)   │                                                │
│            │                                                │
│  - 导航    │   - 画布 / 内容区域                             │
│  - 工具    │   - 浮动面板                                    │
│            │                                                │
├────────────┴────────────────────────────────────────────────┤
│ Footer (可选)                                                │
└─────────────────────────────────────────────────────────────┘
```

### 浮动面板布局

- **工具栏**：底部居中，Mac Dock 风格
- **属性面板**：右侧，可收起
- **导航面板**：左上角，极简设计
- **AI 助手**：右下角浮动球

---

## ✅ 六、可访问性

### 对比度

- 遵循 **WCAG AA** 标准
- 正文文字对比度 ≥ 4.5:1
- 大标题对比度 ≥ 3:1

### 键盘支持

- 全面支持 Tab 导航
- 快捷键系统（⌘K 搜索等）
- `focus-visible` 可见焦点框

```css
:focus-visible {
  outline: 2px solid #FF6B35;
  outline-offset: 2px;
  border-radius: 0.75rem;
}
```

### 选中样式

```css
::selection {
  background-color: #FF6B35;
  color: #FFFFFF;
}
```

---

## 🌙 七、暗色模式支持

```css
.dark {
  --color-background: 0 0% 10%;
  --color-foreground: 0 0% 98%;
  --color-card: 0 0% 15%;
  --color-card-foreground: 0 0% 98%;
  --color-muted: 0 0% 20%;
  --color-muted-foreground: 0 0% 65%;
  --color-border: 0 0% 25%;
}
```

---

## 🔧 八、技术实现

### 依赖栈

- **UI 框架**：shadcn/ui + Radix UI
- **样式方案**：Tailwind CSS + CSS Variables
- **动画库**：tailwindcss-animate
- **图标库**：Lucide React
- **工具函数**：`cn()` (clsx + tailwind-merge)

### CSS 文件结构

```
src/styles/
├── theme.css        # CSS 变量定义
├── animations.css   # 关键帧动画
├── components.css   # 组件层样式
└── utilities.css    # 工具类扩展
```

---

## 📝 九、设计清单

### 新项目启动检查

- [ ] 配置品牌色 Primary-500: `#FF6B35`
- [ ] 配置字体栈（Inter + 系统中文）
- [ ] 设置圆角变量 `--radius: 0.75rem`
- [ ] 配置阴影系统
- [ ] 引入 tailwindcss-animate
- [ ] 实现焦点状态样式
- [ ] 实现选中文本样式
- [ ] 美化滚动条

### 组件开发检查

- [ ] 包含 hover 状态动效
- [ ] 包含 focus-visible 样式
- [ ] 支持键盘交互
- [ ] 遵循品牌色规范
- [ ] 过渡使用标准缓动函数

---

**文档版本**：v1.0  
**提取来源**：LegalMind-Arbitration 1201  
**适用范围**：LawClick ERP / LegalMind 系列产品  
**最后更新**：2025-12-11
